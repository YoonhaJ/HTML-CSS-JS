function if_1 () {
  //1. 사용자로부터 데이터 입력
  inData = prompt("비교값 입력: ")
  //2. 입력이 500보다 크면 '500보다 큰 값입니다' 출력
  //3. 작업을 종료합니다. 멘트 출력
  if (inData >500) {
    document.write('500보다 큰 값입니다.')
  }
    alert('작업을 종료합니다.')
  
}
